all:
	pdflatex trabalho1.tex && bibtex trabalho1 && pdflatex trabalho1.tex && pdflatex trabalho1.tex 

clean:
	rm -rf *.aux *.log *.swp *.out _minted* *.gz *.fls *.fdb_latexmk *.toc *.idx *.lof *.lot *.bbl *.blg *.brf *.dvi
